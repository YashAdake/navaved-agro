# Database (Docker PostgreSQL)
DATABASE_URL=postgresql+asyncpg://yash:yash123@localhost:5432/testdb

# JWT Authentication
JWT_SECRET=navaved-agro-super-secret-key-change-in-production-2026
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Supabase Storage (leave empty for now — image upload won't work until configured)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_BUCKET=product-images
